// src/main.tsx (React + Vite)
import { SDKProvider } from '@twa-dev/sdk';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <SDKProvider>
    <App />
  </SDKProvider>
);

// In App.tsx
useEffect(() => {
  Telegram.WebApp.ready();
  Telegram.WebApp.expand(); // Fullscreen
}, []);